# Source projects are kept local only; the PUBLISHED builds are committed:
#   armonia/     (sursă site carte)  -> build comis în /carte/   (cd armonia; ./publish.ps1)
#   armonia-app/ (sursă aplicație)   -> build comis în /app/     (cd armonia-app; ./publish.ps1)
armonia/
armonia-app/

# Local tooling / other local-only folders
.claude/
node_modules/
